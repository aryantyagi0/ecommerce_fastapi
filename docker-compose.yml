version: "3.8"
services:
  db:
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: postgres.vjdbzczvykhixdlagmsw
      POSTGRES_PASSWORD: Mahiipl9917
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  app:
    build: .
    ports:
      - "8000:8000"
    environment:
      PORT: "8000"
      DATABASE_URL: "postgresql+psycopg2://postgres.vjdbzczvykhixdlagmsw:Mahiipl9917@aws-1-ap-northeast-1.pooler.supabase.com:6543/postgres?sslmode=require"
    depends_on:
      - db
    restart: always

volumes:
  postgres_data:
